# ---- Orbit credentials ----

# Everything now lives in ONE file: backend/credentials.json
# (LLM provider keys, n8n key, webhook URLs, active provider).
# Copy backend/.env.example values there if you still have an old setup.
#
# This .env.example is kept for legacy setups only. New installs:
#   copy credentials.example.json -> backend/credentials.json and fill keys.